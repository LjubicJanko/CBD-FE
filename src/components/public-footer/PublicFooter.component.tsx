import InstagramButton from '../instagram/InstagramButton.component';
import * as Styled from './PublicFooter.styles';

const PublicFooter = () => {
  return (
    <Styled.PublicFooterContainer>
      <InstagramButton />
    </Styled.PublicFooterContainer>
  );
};

export default PublicFooter;
